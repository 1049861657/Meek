'use client';

export function SettingsPageClient(): React.ReactElement {
  return (
    <main className="page-shell settings-page">
      <h1>配置管理</h1>
      <p>提供商与 MCP 配置（M0 路由壳，M3-05 实现）</p>
    </main>
  );
}
